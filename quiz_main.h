#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include "Lifelines.h"
#include "Comments.h"
#include "Max_score.h"
#define MAX 20

void push1(STACK1 *ptr,char* a);
void push2(STACK2 *ptr,int a);
char* pop1(STACK1 *ptr);
void pop2(STACK2 *ptr);
int peepAns(STACK2 *ptr);

